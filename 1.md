You are working on the **AtlasGT** repository.

We have a real E2E failure in the production-style test:

`scripts/queen-live.sh`

Current verified state:

* `dotnet test .\AtlasGT.sln -c Release --no-restore`
* **81/81 tests pass**
* Git working tree is clean
* HEAD and `origin/master` are both:
  `b2e02911bacbc76c8b1e3b64277621db1b810753`
* `scripts/queen-live.sh` successfully performs all previous steps.
* Current live result:
  **PASS=12 FAIL=1**
* The only failing step is:
  **8) Backup E2E**

Observed failure:

```text
== 8) Backup E2E ==
Traceback (most recent call last):
  File "<string>", line 1, in <module>
  ...
json.decoder.JSONDecodeError: Expecting value: line 2 column 1 (char 1)
[FAIL] backup no existe en disco
```

The relevant script code is:

```bash
BK=$(curl -sf -X POST "$API_URL/api/admin/backup" "${H_ADMIN[@]}")
BK_PATH=$(echo "$BK" | python -c 'import json,sys; print(json.load(sys.stdin)["path"])')
[ -f "$BK_PATH" ] && ok "backup creado en $BK_PATH" || bad "backup no existe en disco"
```

The API controller is:

```csharp
[HttpPost("backup")]
public async Task<IActionResult> CreateBackup(CancellationToken ct)
{
    try
    {
        var path = await _backup.CreateBackupAsync(null, ct);
        await Audit("backup.create", path, true, null, ct);
        return Ok(new { path, sizeBytes = new System.IO.FileInfo(path).Length });
    }
    catch (Exception ex)
    {
        await Audit("backup.create", null, false, ex.Message, ct);
        return StatusCode(500, new { error = ex.Message });
    }
}
```

The backup implementation is:

```csharp
public interface IBackupService
{
    Task<string> CreateBackupAsync(string? destinationDir = null, CancellationToken ct = default);
    Task<bool> RestoreAsync(string zipPath, string? destinationDir = null, CancellationToken ct = default);
    Task<IReadOnlyList<string>> ListBackupsAsync(string? destinationDir = null, CancellationToken ct = default);
}

public sealed class FileBackupService : IBackupService
{
    private readonly string _sourceDir;

    public string SourceDirForDebug => _sourceDir;

    public FileBackupService(string sourceDir)
    {
        if (string.IsNullOrWhiteSpace(sourceDir))
            throw new ArgumentException("sourceDir requerido", nameof(sourceDir));

        _sourceDir = sourceDir;
    }

    public async Task<string> CreateBackupAsync(
        string? destinationDir = null,
        CancellationToken ct = default)
    {
        var parent = Directory.GetParent(_sourceDir)?.FullName ?? _sourceDir;
        var tag = Path.GetFileName(_sourceDir.TrimEnd(Path.DirectorySeparatorChar));
        var dest = destinationDir ?? Path.Combine(parent, $".backups-{tag}");

        Directory.CreateDirectory(dest);

        var stamp = DateTime.UtcNow.ToString("yyyyMMdd-HHmmss");
        var fileName = $"atlasgt-backup-{stamp}.zip";
        var fullPath = Path.Combine(dest, fileName);

        await Task.Run(() =>
        {
            System.IO.Compression.ZipFile.CreateFromDirectory(
                _sourceDir,
                fullPath);
        }, ct).ConfigureAwait(false);

        return fullPath;
    }

    public async Task<bool> RestoreAsync(
        string zipPath,
        string? destinationDir = null,
        CancellationToken ct = default)
    {
        if (!File.Exists(zipPath))
            return false;

        var dest = destinationDir ?? _sourceDir;
        Directory.CreateDirectory(dest);

        try
        {
            await Task.Run(() =>
            {
                System.IO.Compression.ZipFile.ExtractToDirectory(
                    zipPath,
                    dest,
                    overwriteFiles: true);
            }, ct).ConfigureAwait(false);

            return true;
        }
        catch (InvalidDataException)
        {
            return false;
        }
        catch (IOException)
        {
            return false;
        }
    }

    public Task<IReadOnlyList<string>> ListBackupsAsync(
        string? destinationDir = null,
        CancellationToken ct = default)
    {
        var parent = Directory.GetParent(_sourceDir)?.FullName ?? _sourceDir;
        var tag = Path.GetFileName(_sourceDir.TrimEnd(Path.DirectorySeparatorChar));
        var dest = destinationDir ?? Path.Combine(parent, $".backups-{tag}");

        if (!Directory.Exists(dest))
            return Task.FromResult<IReadOnlyList<string>>(
                Array.Empty<string>());

        var files = Directory.GetFiles(
            dest,
            "atlasgt-backup-*.zip");

        Array.Sort(files);

        return Task.FromResult<IReadOnlyList<string>>(files);
    }
}
```

Your task:

1. **Do not assume the JSONDecodeError is the root cause.**
   It only indicates that `curl -sf` did not produce valid JSON, potentially because the API returned HTTP 500.

2. Inspect the actual AtlasGT dependency registration in:
   `src/AtlasGT.Api/Program.cs`
   especially:

   ```csharp
   builder.Services.AddSingleton<IBackupService>(sp => ...)
   ```

   Determine the exact `_sourceDir` used by the live API process.

3. Trace the complete backup path:
   `Program.cs → IBackupService → FileBackupService → CreateFromDirectory()`

4. Determine the actual reason `/api/admin/backup` fails during `queen-live.sh`.
   Do not guess.

5. Reproduce the failure locally and capture the actual API error before changing code.

6. Pay special attention to:

   * whether `_sourceDir` exists at runtime;
   * whether `_sourceDir` is readable;
   * whether the destination directory is valid;
   * whether the destination is accidentally inside the source tree;
   * Windows/Git-Bash path differences;
   * file locking;
   * permissions;
   * cancellation;
   * backup filename collisions;
   * whether the source contains files that `ZipFile.CreateFromDirectory()` cannot read;
   * whether the live test's temporary `AtlasGT__DataRoot` changes the expected directory structure.

7. Fix the **root cause in AtlasGT**, preferably in the appropriate production code rather than weakening the test.

8. The backup implementation must remain compatible with:

   * automated unit tests;
   * Windows;
   * the live API process;
   * future multiple backup executions;
   * restore/list functionality.

9. If the timestamp-based filename is part of the problem, make backup filenames collision-safe rather than merely sleeping or retrying blindly.

10. Do not remove, skip, weaken, or rewrite the backup assertion just to make `queen-live.sh` green.

11. Do not modify unrelated modules.

12. After the fix:

* run the relevant backup/unit tests;
* run the complete solution test suite;
* run `scripts/queen-live.sh` from the same Git Bash environment where it previously failed;
* verify that the final result is:
  `PASS=13 FAIL=0`
* inspect `git diff`;
* ensure no temporary runtime data or generated backups are accidentally committed.

13. If the script itself also needs a robustness improvement, it may be improved **after** the production backup problem is fixed. The script should expose the HTTP response/error when the backup endpoint fails instead of producing only a Python JSONDecodeError.

Important workflow:

**read → reproduce → diagnose actual exception → edit minimal files → build → tests → queen-live → read/verify → git diff**

Do not stop at diagnosis if a safe root-cause fix is identified.

At the end, report in Spanish:

* causa exacta;
* archivo(s) modificados;
* cambio realizado;
* tests ejecutados;
* resultado de `queen-live.sh`;
* whether the working tree is clean.

Do not claim success unless the commands actually pass.
